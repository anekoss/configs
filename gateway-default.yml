server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: auth
          uri: http://auth:8085
          predicates:
            - Path=/users/**,/auth/**,/admin/**,swagger-ui/**,/v3/api-docs/**,/webjars/**
        - id: department
          uri: http://department:8086
          predicates:
            - Path=/departments/**,/rooms/**,swagger-ui/**,/v3/api-docs/**,/webjars/**
        - id: order
          uri: http://order:8087
          predicates:
            - Path=/orders/**,swagger-ui/**,/v3/api-docs/**,/webjars/**
        - id: oxygen
          uri: http://oxygen:8096
          predicates:
            - Path=/storages/**,/oxygen-supply/**,swagger-ui/**,/v3/api-docs/**,/webjars/**
        - id: file
          uri: lb://file
          predicates:
            - Path=/files/**,swagger-ui/**,/v3/api-docs/**,/webjars/**

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: refresh